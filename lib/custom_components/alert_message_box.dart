import 'package:flutter/material.dart';

class AlertMessageBox extends StatelessWidget {
  const AlertMessageBox({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
